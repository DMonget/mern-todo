const express = require ('express');
const router = express.Router();

router.get('/todos', (res, res, next) => {});
router.post('/todos', (res, res, next) => {});
router.delete('/todos/:id', (res, res, next) => {});

module.exports = router;